# Render Blueprint - SkillTen / Viya AI
# Deploy both backend and frontend from one repo

services:
  # Backend API (FastAPI)
  - type: web
    name: skilleton-api
    runtime: python
    region: oregon
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        value: sqlite:///./skillsync.db
      - key: PYTHON_VERSION
        value: "3.11.0"
      - key: ENV
        value: production

  # Frontend (Next.js) — if deploying on Render instead of Vercel
  - type: web
    name: skilleton-web
    runtime: node
    region: oregon
    rootDir: frontend
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NEXT_PUBLIC_BACKEND_URL
        fromService:
          name: skilleton-api
          type: web
          property: hostport
      - key: NODE_VERSION
        value: "20.11.0"

